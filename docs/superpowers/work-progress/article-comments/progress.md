# Article Comments Progress

## 目标
- 完成前台文章评论、后台评论管理和评论数据域的分阶段交付，并通过评论功能相关质量门禁与 E2E 验收。

## 当前阶段
- 验收阶段完成（评论范围与全量 E2E 门禁均已通过）。
- 进入收口准备（整理合并建议与残余风险说明）。

## 已完成项
- 完成输入校验、需求澄清与设计文档沉淀。
- 完成三份独立实施计划（数据域、前台 UI、后台管理）。
- 完成隔离开发准备（目标分支 + 子任务隔离 worktree/子分支）。
- 数据域实现已集成：评论模型、评论状态、评论 API、服务层能力。
- 前台评论 UI 已集成：列表展示、游客/登录用户身份展示、关闭评论时输入区隐藏。
- 后台评论管理已集成：菜单入口、评论列表、按文章筛选、管理员删除。
- 完成验收阶段失败簇收敛：
  - 将匿名后台访问校验改为稳定的 API 401 断言，消除 UI 重定向竞态。
  - 为发布缓存链路请求增加超时容错与重试，降低远程 DB 抖动影响。
  - 为 `loginAsAdmin` helper 增加重试与退避，降低并发下登录瞬时失败。
- 评论范围与全量门禁均已通过。

## 进行中
- 无。

## 阻塞项
- 无硬阻塞。
- 远程 PostgreSQL（Supabase）偶发连接抖动仍是环境风险，但本轮未再阻断门禁。

## 下一步
- 进入收口：提交合并建议与未验证专项清单（并发性能、长稳回归）。
- 如需进一步降低环境抖动影响，可增加 CI 侧重试策略或本地化测试数据库方案。

## 验证证据
- npm run lint -> PASS
- npm run test:e2e -- tests/e2e/admin-auth.spec.ts tests/e2e/admin-publish-cache.spec.ts -> PASS（9 passed）
- npm run test:e2e -> PASS（32 passed, 0 failed）

## 已验证项
- 评论关闭时禁止创建评论。
- 游客评论与登录用户评论显示身份差异。
- 评论作者本人可删，管理员可删。
- 后台评论按文章筛选与删除流程可用。
- `admin-auth` 匿名鉴权与会话链路在全量执行下通过。
- `admin-publish-cache` 发布/更新/下线链路在全量执行下通过。
- 全量 E2E 门禁通过（32/32）。

## 未验证项
- 评论相关并发性能专项（高并发写入/删除压力）。
- 跨时段长稳回归（多轮连续全量执行）。
